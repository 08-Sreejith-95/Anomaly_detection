import argparse

'''
parser  = argparse.ArgumentParser()
parser.add_argument('-f', '--segmentationfile', type = str, help = 'Segmented slice')
parser.add_argument('-p', '--indexpath', type = int, help = 'To extract path of the file stored in path list')
parser.add_argument('-s',  '--slice', type = int, help = 'To get 2Dslice of the input segmentation')
args = parser.parse_args()
'''

